# Configuração para Produção (VertraCloud)

# Ambiente
NODE_ENV=production

# Porta do servidor (configure conforme a VertraCloud)
PORT=3000

# Chave secreta para sessões (GERE UMA NOVA E FORTE!)
# Use: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=sua_chave_secreta_aqui_minimo_32_caracteres

# Google AdSense (Opcional - para monetização)
# GOOGLE_ADSENSE_ID=ca-pub-XXXXXXXXXXXXXXXX
